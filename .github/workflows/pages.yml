name: Publish project website
on:
  push:
    branches: [main]
    paths: ['pages/**', 'index.html', '404.html', 'site-premium.css', 'site-data.js', 'portfolio-content.js', 'portfolio-app.js', 'favicon.svg', 'assets/icons/**', '.github/workflows/pages.yml']
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          package-manager-cache: false
      - run: node pages/build.mjs && node pages/check.mjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: pages/public
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
